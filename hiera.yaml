---
:backends:
  - yaml
 :hierarchy:
   - "%{clientcert}"
   - "%{environment}"
   - "nodes/%{clientcert}"
   - common

:yaml:
  :datadir: '/etc/puppetlabs/code/environments/%{::environment}/hieradata'
